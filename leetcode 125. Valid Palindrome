class Solution {
    public boolean isPalindrome(String s) {
        String results = s.replaceAll("[^a-zA-Z0-9]", "").toLowerCase();
        StringBuilder sb = new StringBuilder(results);
        String another = sb.reverse().toString();
        if(results.equals(another)) {
            return true;
        }
    return false;
    }
}
